These are sample configs originally created by the template but not normally included in the git repo. They are saved here to be used as samples after pulling git to a new machine.

copy dev-config.edn and test-config.edn to the top dir of the project. You may want to customize the ports if
there are going to be mulitple instances of your services running on the same machine.
